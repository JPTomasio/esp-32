# Configuracao com a senha do WiFi. NAO versionar: o repositorio e publico.
# Use config.exemplo.h como modelo.
firmware/02_monitor_postura/config.h

# Credenciais do Supabase. NAO versionar: o repositorio e publico e a chave
# service_role da acesso total ao banco. Use .env.exemplo como modelo.
servidor/.env

# Documentos das entregas. NAO versionar: os enunciados em PDF sao material do
# professor e o documento do grupo traz nome completo e matricula dos seis
# integrantes -- este repositorio e publico.
docs/

# Banco de dados gerado em tempo de execucao
servidor/*.db

# Evidencias geradas por evidencias/gera_evidencias.sh. O script e versionado,
# a saida nao: e regenerada em um minuto e inclui um banco binario.
evidencias/saida/

# Python
__pycache__/
*.py[cod]
venv/
.venv/
# Ambiente criado por evidencias/gera_evidencias.sh
.venv-evidencias/

# Sistema
.DS_Store
